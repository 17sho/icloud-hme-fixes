# Caddy reverse proxy for icloud-hme.
# Replace "hme.example.com" with your real domain. Caddy issues a TLS cert automatically.
# The upstream service listens on 127.0.0.1:5050 (never bind it directly to the internet).

hme.example.com {
    reverse_proxy 127.0.0.1:5050
}
